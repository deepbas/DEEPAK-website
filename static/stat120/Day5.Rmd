---
title: "Describing Variables"
subtitle: "<br/> STAT 120"
author: "Bastola"
date: "`r format(Sys.Date(), ' %B %d %Y')`"
output:
  xaringan::moon_reader:
    css: ["default", css/xaringan-themer-solns.css, css/my-theme.css, css/my-font.css]
    lib_dir: libs
    chakra: libs/remark-latest.min.js
    seal: false
    nature:
      highlightStyle: googlecode  #http://arm.rbind.io/slides/xaringan.html#77 # idea, magula, googlecode, default
      highlightLines: true
      highlightLanguage: ["r", "css", "yaml"]
      countIncrementalSlides: true
      slideNumberFormat: "%current%"
      titleSlideClass: ["left", "middle", "inverse"]
      ratio: "16:9"
    includes:
      in_header: header.html  
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE}
options(htmltools.dir.version = FALSE)
options(htmltools.preserve.raw = FALSE)
options(ggrepel.max.overlaps = Inf)

knitr::opts_chunk$set(echo = TRUE, 
                      dev = 'svg',
                      collapse = TRUE, 
                      comment = NA,  # PRINTS IN FRONT OF OUTPUT, default is '##' which comments out output
                      prompt = FALSE, # IF TRUE adds a > before each code input
                      warning = FALSE, 
                      message = FALSE,
                      fig.height = 3, 
                      fig.width = 4,
                      out.width = "100%"
                      )



# load necessary packages
library(tidyverse)
library(countdown)
library(mosaic)
library(ggthemes)
library(xaringanExtra)
library(forcats)
xaringanExtra::use_panelset()
xaringanExtra::use_tachyons()
xaringanExtra::use_clipboard()
xaringanExtra::use_extra_styles(
  hover_code_line = TRUE,         #<<
  mute_unhighlighted_code = TRUE  #<<
)
library(flipbookr)
library(patchwork)
library(Lock5Data)

# Set ggplot theme
theme_set(theme_tufte(base_size = 10))


yt <- 0

# read.csv("https://raw.githubusercontent.com/deepbas/statdatasets/main/APM_DougEvansCases.csv")
survey <- read.csv("https://raw.githubusercontent.com/deepbas/statdatasets/main/StudentSurvey.csv")
# renderthis::to_pdf(from = "Day4.html", complex_slides = TRUE, partial_slides = TRUE)
```



```{r xaringanExtra-clipboard, echo=FALSE}
htmltools::tagList(
  xaringanExtra::use_clipboard(
    button_text = "<i class=\"fa fa-clipboard\"></i>",
    success_text = "<i class=\"fa fa-check\" style=\"color: #90BE6D\"></i>",
    error_text = "<i class=\"fa fa-times-circle\" style=\"color: #F94144\"></i>"
  ),
  rmarkdown::html_dependency_font_awesome()
)
```


layout: true
  
<!-- <div class="my-footer"><span>Stat 230</span></div> -->
<!-- this adds the link footer to all slides, depends on my-footer class in css-->

---

class: title-slide, middle
<!-- background-image: url("assets/title-image2.jpg") -->
background-position: 10% 90%, 100% 50%
background-size: 160px, 100% 100%

# .fancy[Describing Variables]

### .fancy[Stat 120]

`r format(Sys.Date(), ' %B %d %Y')`

---

# Student Survey

.hljs[Dataset on 362 responses to a student survey given at one college]

.font80[
```{r, echo=FALSE}
knitr::kable(head(survey |> select(-c(4,5,6, 13,14, 15, 18)),10))
```
]

---

class: middle

# Distribution

> In the given dataset, there are multiple variables (such as Gender, Smoke, Exercise, Height, Weight, etc.) for different students across various academic years.


.hljs[Knowing the distribution of each variable can help us identify relationships, trends, and potential anomalies in the data.]

---

class: middle

# Understanding distribution

.bql[
Understanding the distribution of the data is useful for several reasons:

- Descriptive analysis:
- Data quality:
- Assumption checking:
- Interpretation of results: 
- Visualization:
]

---

class: middle

# Distribution 

.bql[

.green-h["The distribution of the variable Y"]

- describes its center, variability and shape
- use both numbers and graphics

]



---

## Center: Mean or Average

.bql[
.b[Mean:] average value in a sample or population

- $\bar{y} = \dfrac{\sum_{i=1}^ny_i}{n}$ is an average of $n$ values $y_i$ in a sample
- $\mu$ is an average value of $y$ in a population
]

--

## Student Survey 


```{r}
survey <- read.csv("https://raw.githubusercontent.com/deepbas/statdatasets/main/StudentSurvey.csv")
mean(survey$Pulse) # the command `mean` computes an average
```

.hljs[
The mean pulse rate for this sample of students is $\bar{y} = 69.6$ beats per minute.
]

---

# Center: Median
.bql[
.bold[Median:] the middle value when the data are ordered
- The median splits the data in half
- $m$ is the median value in a sample
- $M$ is the median value in a population
]

--

```{r}
median(survey$Pulse) # the command `median` computes an median
```


.hljs[
The median pulse rate for this sample of students is $m = 70$ beats per minute. 
]

---

# Variability: Standard Deviation

.bql[
Standard Devation (SD): average value in a sample or population
- $s = \sqrt{ \dfrac{\sum_{i=1}^n(y_i - \overline{y})^2}{n-1}}$ is the SD of $n$ values $y_i$ in a sample
- $\sigma$ is the SD of values of $y$ in a population
]

```{r}
sd(survey$Pulse) # the command `sd` computes an average
```


.hljs[
The SD of pulse rates for this sample of students is $s = 12.2$ beats per minute. The "average" deviation of individual pulse rates around the mean value is about 12.2 beats per minute.
]

---

class: middle

# Shape: histogram

.bqt[.bold[Histogram:] aggregates values into bins and counts how many cases fall into each bin]

.pull-left[

.code80[
```{r, echo = FALSE, fig.height=3.5, out.width="70%", fig.align='center'}
ggplot(survey, aes(x = Pulse)) +
  geom_histogram(binwidth = 5) +
  labs(
    title = "Histogram of pulse rates",
    x = "Pulse",
    y = "Count"
  ) +
  theme(
    plot.title = element_text(size = 10),
    axis.title = element_text(size = 7),
    axis.text = element_text(size = 7)
  )
```
]
]
.pull-right[
.bql.font80[
- Pulse rates are .bold[symmetrically] distributed around a rate of about 70 beats per minute.

- Symmetric distributions are "centered" around a mean and median that are roughly the same in value.

]
]

---

# Shape and Stats

.pull-left[
.bqt.font90[Mean and standard deviation are good summary stats of a .bold[symmetric] distribution.]

```{r, echo=FALSE}
ggplot(survey, aes(x=Pulse)) +
  geom_histogram(fill="steelblue", color="black") +
  labs(title="Histogram of Pulse Rates", cex.lab=0.5, cex.main=0.5, cex.axis=0.5)
```
]
.pull-right[

.bqt.font90[Similar variation to the left and right of the mean so one measure of SD is fine.]

<br>

```{r}
# mean
mean(survey$Pulse)
```

```{r}
# standard deviation
sd(survey$Pulse)
```

]

---

class: middle

# Shape: data distribution

.bql[
If a distribution of data is .bold[approximately bell-shaped], about 95% of the data should fall within two standard deviations of the sample mean.

- for a sample: 95% of values between $\bar{y} - 2s$ and $\bar{y} + 2s$
- for a population: 95% of values between $\mu - 2\sigma$ and $\mu + 2\sigma$
]

---

# Bell-shaped distribution and standard deviation


```{r}
sleep <- read.csv("https://raw.githubusercontent.com/deepbas/statdatasets/main/SleepStudy.csv")
```

.pull-left[

```{r, echo=FALSE}
ggplot(data = sleep, aes(x = AverageSleep)) +
  geom_histogram(fill = "steelblue") +
  labs(title = "Distribution of Sleep Hours",
       cex.lab = 0.5, cex.main = 0.5, cex.axis = 0.5)
```

]

.pull-right[
<br>
.bql[
.bold[Question] The standard deviation for hours of sleep per night is closest to

(a) 0.5

(b) 1

(c) 2

(d) 4

]
]

---

# Standardizing data: z-score

.bqt.font90[The z-score of a data value, x, tells us how many standard deviations the value is above or below the mean:

$$z = \dfrac{x - \text{mean}}{\text{SD}}$$]

<br>

- E.g. if a value $x$ has $z=-1.5$ then the value $x$ is .bold[1.5 standard deviations below] the mean.

.bql[.bold[Question:] If we standardize all values in a bell-shaped distribution, 95% of all z-scores fall between what values?]


---

# Shape: Left Skew $\&$ Right Skew

.pull-left[

```{r, echo=FALSE}
ggplot(data = survey, aes(x = GPA)) + 
  geom_histogram(fill="steelblue", color = "black") +
  labs(title = "Histogram of GPA",
       cex = 0.5)
```

.code50[
```{r}
mean(survey$GPA, na.rm =T)
median(survey$GPA, na.rm = T)
```
]

]

.pull-right[

```{r, echo=FALSE}
ggplot(data = survey, aes(x = Exercise)) + 
  geom_histogram(fill="steelblue", color = "black") +
  labs(title = "Histogram of Exercise",
       cex = 0.5)
```

.code50[
```{r}
mean(survey$Exercise, na.rm =T)
median(survey$Exercise, na.rm = T)
```
]
]

---

# Shape: Left Skew $\&$ Right Skew (Boxplots)

.pull-left[

```{r, echo=FALSE}
ggplot(data = survey, aes(x = 1, y = GPA)) + 
  geom_boxplot() +
  labs(x = "", y = "GPA") + coord_flip()
```

.code50[
```{r}
mean(survey$GPA, na.rm =T)
median(survey$GPA, na.rm = T)
```
]

]

.pull-right[

```{r, echo=FALSE}
ggplot(data = survey, aes(x = 1, y = Exercise)) + 
  geom_boxplot() +
  labs(x = "", y = "Exercise") + coord_flip()
```

.code50[
```{r}
mean(survey$Exercise, na.rm =T)
median(survey$Exercise, na.rm = T)
```
]
]

---

# Shape: boxplots

.bq.font90[.b[Boxplots:] A graphical representation of the distribution of a dataset, showing the median, quartiles, and outliers.]

.bql.font80[
- .b[Box:] Represents the interquartile range (IQR) between the 1st quartile (Q1) and the 3rd quartile (Q3)
- .b[Median:] The middle value of the dataset, represented by a line inside the box
- .b[Whiskers:] Extend from the box to the minimum and maximum data points within 1.5 times the IQR
- .b[Outliers:] Data points outside of the whiskers, often represented as individual points
]

---

class: middle

# Shape: boxplots

.bql[
.b[Symmetry:] If the median is roughly centered within the box, and the whiskers are of similar length, the distribution is likely symmetric.
]

<br>

.bql[.b[Skewness:]

- .bold[Left-skewed:] The median is closer to the upper quartile (Q3), and the left whisker is longer than the right whisker.
- .bold[Right-skewed:] The median is closer to the lower quartile (Q1), and the right whisker is longer than the left whisker.
]


---

## Adding a categorical variable: stats

.bql.font70[Use `group_by()` and `summarise()` to get summary statistics using `dplyr` package to compare distributions across different levels of a categorical variable
]


```{r}
survey %>%
  group_by(Smoke) %>%
  summarise(
    Min = min(Pulse, na.rm = TRUE),
    Q1 = quantile(Pulse, 0.25, na.rm = TRUE),
    Median = median(Pulse, na.rm = TRUE),
    Mean = mean(Pulse, na.rm = TRUE),
    Q3 = quantile(Pulse, 0.75, na.rm = TRUE),
    Max = max(Pulse, na.rm = TRUE),
    SD = sd(Pulse, na.rm = TRUE),
    N = n() )
```


.hljs[
Pulse rate stats by smoking status: Smoker have a slightly higher mean pulse rate than non-smokers (71.8 vs. 69.3).
]

---

`r chunk_reveal("cat-graphics-1", widths = c(0.51, 0.49), title = "## Adding a categorical variable: graphics")`

```{r cat-graphics-1, fig.width = 3, fig.height = 3.5, out.width = "100%", include=FALSE}
library(ggplot2)
ggplot(survey, aes(x=Pulse)) + 
  geom_histogram(fill="steelblue") +
  facet_wrap(~Smoke)

```

---

.panelset[

.panel[
.panel-name[Side-by-side boxplot]

```{r echo=FALSE, fig.height=3, out.width="60%", fig.align='center'}
ggplot(survey, aes(x = Smoke, y = Pulse)) +
  geom_boxplot() +
  facet_wrap(~ Smoke, scales = "free_x") +
  labs(x = "Smoking Status", y = "Pulse Rate") +
  theme(axis.text.x = element_text(angle = 90, vjust = 0.5, hjust = 1))
```
]


.panel[
.panel-name[Code]

```{r eval=FALSE, fig.height=3, out.width="60%", fig.align='center'}
# Create boxplots using facet_wrap()
ggplot(survey, aes(x = Smoke, y = Pulse)) +
  geom_boxplot() +
  facet_wrap(~ Smoke, scales = "free_x") +
  labs(x = "Smoking Status", y = "Pulse Rate")
```
]

]



---



class: action

# <i class="fa fa-pencil-square-o" style="font-size:48px;color:purple">&nbsp;Your&nbsp;Turn&nbsp;`r (yt <- yt + 1)`</i>    

.pull-left-40[
![](https://media.giphy.com/media/RKApDdwsQ6jkwd6RNn/giphy.gif)
]
.pull-right-60[


<br>
<br>
.bq[

- Please go over the in-class activity 
- Talk to your neighbor
- Let me know if you have any questions
]
]

`r countdown(minutes = 10, seconds = 00, top = 0 , color_background = "inherit", padding = "3px 4px", font_size = "2em")`


